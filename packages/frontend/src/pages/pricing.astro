---
// packages/frontend/src/pages/pricing.astro
import Layout from "../layouts/Layout.astro";
import PricingPage from "algolens-premium-frontend/components/PricingPage";
import { pb } from "../auth/pocketbase";

import { BACKEND_URL } from "astro:env/client";
console.log("env", BACKEND_URL);
const env = {
  BACKEND_URL,
  PADDLE_CLIENT_TOKEN: import.meta.env.PUBLIC_PADDLE_CLIENT_TOKEN,
  PADDLE_MONTHLY_PRODUCT_ID: import.meta.env.PUBLIC_PADDLE_MONTHLY_PRODUCT_ID,
  PADDLE_YEARLY_PRODUCT_ID: import.meta.env.PUBLIC_PADDLE_YEARLY_PRODUCT_ID,
  PADDLE_ENVIRONMENT: import.meta.env.PUBLIC_PADDLE_ENVIRONMENT,
};

// Note: User email is now accessed directly in the PricingPage component
---

<Layout title="Pricing - AlgoLens">
  <PricingPage client:load env={env} />
</Layout>
