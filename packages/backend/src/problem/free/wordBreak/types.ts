/**
 * Defines the input structure for the Word Break problem.
 */
export interface WordBreakInput {
  /** The string to be segmented. */
  s: string;
  /** A list of dictionary words. */
  wordDict: string[];
}
