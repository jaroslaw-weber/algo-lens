// Note: The original file name is non-overlapping-intervals.ts, but the interface is EraseOverlapIntervalsInput.
// We'll keep the interface name as defined in the original file.
export interface EraseOverlapIntervalsInput {
  intervals: number[][];
}
