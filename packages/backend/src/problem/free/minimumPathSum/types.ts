// Note: The original file also defined MinPathSumState, but that's derived from ProblemState
// and likely doesn't need its own type definition in the new structure.
// We only define the input type here.
export interface MinPathSumInput {
  grid: number[][];
}
