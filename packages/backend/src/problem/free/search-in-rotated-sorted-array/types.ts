/**
 * Defines the input structure for the Search in Rotated Sorted Array problem.
 */
export interface SearchRotatedInput {
  /** The rotated sorted array. */
  nums: number[];
  /** The value to search for. */
  target: number;
}
