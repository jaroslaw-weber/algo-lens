/**
 * Defines the types specific to the Minimum Window Substring problem.
 */
export interface MinimumWindowSubstringTypes {
  // No complex types needed for inputs (s: string, t: string) or output (string)
  // Add any specific types for visualization or internal data structures here if needed.
}
