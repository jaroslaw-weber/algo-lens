# algo-lens-core

The core library for AlgoLens, providing essential types, utilities, and classes for algorithm visualization including problem definitions, state management, variable types (arrays, numbers, strings, trees, linked lists, etc.), step logging, linked list serialization, and error handling.

All packages in this monorepo use Bun as the JavaScript runtime.