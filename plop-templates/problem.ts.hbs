// Can check how to implement here: packages/backend/src/problem/guidelines/problem-guidelines.md
import { Problem, ProblemState } from "algo-lens-core/src/types";
import { variables } from './variables';
import { getSteps } from './steps';
import { testcases } from './testcase';
import { groups } from './groups';
import { {{properCase name}}Input } from './types';

export const problem: Problem<{{properCase name}}Input, ProblemState> = {
  title: '{{properCase name}}',
  emoji: '💡', // Default emoji, can be changed later
  func: getSteps,
  testcases: testcases,
  difficulty: 'easy', // Default difficulty, can be changed later
  id: '{{kebabCase name}}',
  tags: [], // Default tags, can be changed later
  metadata: {
    variables,
    groups,
  },
  codegen: {
    signature: '{{camelCase name}}({{camelCase name}}Input: {{properCase name}}Input)',
  },
};